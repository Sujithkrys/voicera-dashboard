{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "providers": ["python", "node"]
    }
  },
  "deploy": {
    "startCommand": "uvicorn main:fastapi_app --host 0.0.0.0 --port ${PORT:-8000}"
  }
}
